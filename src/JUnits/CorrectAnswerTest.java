/*package JUnits;

import Model.Difficulty;
import Model.Question;
import org.junit.Test;

import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

public class CorrectAnswerTest {

    @Test
    public void testCheckCorrect() {
        Question question = new Question("Exsample Question", "Option1", "Option2", "Option3", "Option4", "Option1", Difficulty.EASY);

        assertTrue(question.checkCorrect("Option1"));
        assertFalse(question.checkCorrect("Option2"));

    }
}*/